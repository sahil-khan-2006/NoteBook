# PostgreSQL Database Connection String
# Local PostgreSQL example:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/app_db"
#
# Cloud PostgreSQL example (e.g. Neon, Supabase, Railway, Render, etc.):
# DATABASE_URL="postgresql://username:password@your-host.provider.com:5432/dbname?sslmode=require"
DATABASE_URL="postgresql://neondb_owner:npg_OZqVuJnvy91C@ep-shy-credit-b4jke1nk.c-6.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Authentication Security Settings (Optional, defaults exist in code)
AUTH_PEPPER="bpmandal-hub-pepper-v1"
SESSION_SECRET="bpmandal-hub-session-secret-change-in-prod"

# Optional: Restrict registration to college domain (e.g. "bpmandal.ac.in")
# COLLEGE_EMAIL_DOMAINS="bpmandal.ac.in"

# Optional: File uploads directory (defaults to ./uploads in the project root)
# UPLOAD_DIR="./uploads"
